# --- core (required) ---
fastapi<0.113
uvicorn[standard]>=0.30
pydantic>=2.7
python-multipart>=0.0.9

# --- language model (optional: without it the extractive composer is used) ---
anthropic>=1.0

# --- PostgreSQL + pgvector (optional: without it the in-memory index is used) ---
sqlalchemy>=2.0
psycopg[binary]>=3.1

# --- document ingestion (optional: PDF and OCR support) ---
pypdf>=4.2
# OCR for scanned pages also needs the Tesseract and Poppler binaries installed:
# pytesseract>=0.3.10
# pdf2image>=1.17

# --- real sentence embeddings (optional: EMBEDDING_PROVIDER=sbert) ---
# sentence-transformers>=3.0
